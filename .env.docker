# ── Docker Environment Variables ─────────────────────────────────
# Copy this file as .env in the project root when using Docker:
#   cp .env.docker .env
# Then run: docker compose up --build

SECRET_KEY=django-insecure-clean-secret-key-without-dollar-signs-1234567890
DB_NAME=jobboard_db
DB_USER=postgres
DB_PASSWORD=root
DB_HOST=db
DB_PORT=5432
DEBUG=True
